# system.md

> Generated/updated by `/stack-eval` P10. Describes how the system is built and operated.

## System overview

<!-- Fill: product name, monorepo layout, primary apps, shared libs -->

## Runtime topology

<!-- Fill: SPA ports, API port, DB, proxies, production hosts (e.g. Vercel) -->

## Data flow

<!-- Fill: client → API → DB / external services -->

## Environment

| Variable | Required | Used by | Notes |
|----------|----------|---------|-------|
| | | | |

## Build & quality

| Command | Purpose |
|---------|---------|
| `pnpm run typecheck` | Typecheck (primary gate) |
| `pnpm run build` | Production builds |

## Agent operating contract

- Read `CLAUDE.md` / `AGENTS.md` / this file before large changes.
- Update documentation **before / during / after** work (see `rules.md`).
- Use `.agent/` for run state (`tasks.json`, `progress.md`, `state.json`).
- Do not mark work complete without orchestrator verification gates when under `/stack-eval`.

## Related docs

- `STRATEGY.md` — strategic direction (via `ce-strategy`)
- `rules.md` — hard rules
- `.agent/end-state.md` — current eval target
- `.agent/MASTER_PLAN.md` — approved execution plan (when present)

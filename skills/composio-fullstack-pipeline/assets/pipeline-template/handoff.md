# Handoff — __PROJECT_NAME__

**Last updated:** __TIMESTAMP__

## What this project is
<one paragraph from the planner — fill in after Phase 1>

## What's deployed
- Production: not yet
- Preview: not yet
- Repo: not yet

## What's next
Run the planner to populate tasks.json.

## Who pays for what
<populate after Phase 2 when integrations are live>

## Credential locations
<populate after Phase 2>

## Known issues / backlog
<populate as P2/P3 evaluator findings accumulate>

## To resume this project
1. cd into the repo.
2. Verify auth: `echo $ANTHROPIC_API_KEY` should be empty; `/status` should show Subscription.
3. Read `.agent/state.json` for current moment.
4. Read `.agent/progress.md` tail for last 5 events.
5. Pick up at `state.json.next_action`.
